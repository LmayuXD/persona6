document.addEventListener("DOMContentLoaded", () => {
    const creditos = document.getElementById("creditos");

    setTimeout(() => {
        creditos.style.animation = "subir 20s ease-in-out forwards";
    });
});